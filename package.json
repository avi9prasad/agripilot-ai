{
  "name": "agripilot-ai",
  "version": "1.0.0",
  "description": "AI farm advisor — soil, weather & crop data in, Claude-powered irrigation/fertilizer/pest recommendations out. Built for the WeMakeDevs x Zerops Challenge.",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run install:all && npm run build --prefix client",
    "start": "node server/src/index.js",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client"
  },
  "engines": {
    "node": ">=20"
  }
}
